
export const TokenPoolEvents = {
    SWAP : "Swap",
    TRANSFER : "Transfer",
    BURN :"Burn",
    MINT :"Mint",
    SYNC :"Sync",
}

export enum ERC20Events {
    TRANSFER = "Transfer",
    APPROVAL = "Approval",

}

export enum UniswapFactoryEvents {
    PAIR_CREATED = "PairCreated"
}